## P1 Features — Implemented (Phase 4C)

### Commands added to `hts`

| Command | Action | Status |
|---------|--------|--------|
| `hts --filters` | List snip filters (reads from sync or filter-map) | ✅ |
| `hts --packs` | List hts packs with filters/aliases | ✅ |
| `hts --sync-snip-filters [--from /path/to/snip]` | Sync filter inventory from local snip repo | ✅ |

### Behavior

**hts --filters**
- Priority: read from `$SYNC_SNIP_FROM/filters/` → fallback to `config/filter-map.yaml` → error
- Source default: `/home/arkhi25/Repo/references/agentic/snip`
- Output: sorted list (127 filters)

**hts --packs**
- Reads `packs/*.yaml`
- Shows: name, description, filters, aliases

**hts --sync-snip-filters**
- Scans `$SYNC_SNIP_FROM/filters/*.yaml`
- Writes sorted list to `config/snip-filters.txt`
- Count + output preview

### Files touched

- `scripts/hts`:
  - added flags: `--filters`, `--packs`, `--sync-snip-filters [--from ...]`
  - added env: `HTS_SNIP_REF` (default: `/home/arkhi25/Repo/references/agentic/snip`)
  - added functions: `list_filters()`, `list_packs()`, `sync_snip_filters()`
  - updated usage/help text

- `config/snip-filters.txt`: generated by sync (127 entries)

### Test results

✓ `bash -n` ok  
✓ `hts --filters` → 127 lines  
✓ `hts --packs` → docker/git/node/python output with filters/aliases  
✓ `hts --sync-snip-filters` → writes to config/snip-filters.txt  

---